"use client";

import SignupPage from "../signup/page";

// Re-use standard SignupPage for /register to avoid code duplication and errors
export default function RegisterPage() {
    return <SignupPage />;
}
